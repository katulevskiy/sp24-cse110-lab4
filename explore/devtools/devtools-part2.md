1. What was the bug? Concatenating strings instead of adding integers.
2. How would you fix it? Added + before each string when doing addition, which converts them to an integer before adding together.
